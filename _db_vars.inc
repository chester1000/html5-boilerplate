<?

    $db = Array();
    $db['on'] = false;

    if($db['on']) {
        $db['host'] = "";
        $db['name'] = "";
        $db['user'] = "";
        $db['pass'] = "";

        $db['fb_in_db'] = false; // are fb settings stored in db 
    }

?>
